2026-03-14: Added executable contract baseline via `test/nex/agent/layer_contract_helper.exs` and assertions in context/user tests so all six layers now have explicit allowed/forbidden boundaries in code-adjacent tests.
2026-03-14: Encoded policy text that read/compose should emit diagnostics for out-of-layer content while invalid new writes are rejected, without changing runtime behavior in this task.
2026-03-14: Added `Nex.Agent.ContextDiagnostics` as a shared diagnostics primitive used by prompt composition (`ContextBuilder`) and write validation (`soul_update`, `user_update`) with stable category atoms and deterministic messages.
2026-03-14: Characterization tests now cover at least four boundary categories including `identity_declaration_in_soul`, `user_profile_data_in_soul`, `persona_style_instruction_in_memory`, and `outdated_capability_model_claim_in_agents`.
2026-03-14: Added characterization coverage in `test/nex/agent/context_builder_test.exs` for conflicting `AGENTS.md`/`SOUL.md`/`USER.md` bootstrap inputs, asserting prompt keeps canonical `Nex Agent` identity text while preserving user-authored file content unchanged.
2026-03-14: Characterized diagnostics surface through `ContextBuilder.build_system_prompt_diagnostics/1` with stable expected shape (`category`, `source_layer`, `source`, `severity`, `message`) for outdated AGENTS capability-model claims, SOUL identity declarations, and USER persona/identity directives.
2026-03-14: `soul_update` now routes writes through layer diagnostics and rejects identity declaration payloads with a deterministic `identity_declaration_in_soul` error instead of acting as unrestricted overwrite.
2026-03-14: `user_update` preserves append/set profile ergonomics while enforcing boundary validation (via `ContextDiagnostics.validate_write(:user, ...)`) so persona/identity attempts fail before touching USER.md.
2026-03-14: Tool descriptions were tightened to match runtime behavior (`soul_update`, `user_update`, `memory_write`) so model-facing guidance no longer implies identity-rewrite capability.
2026-03-14: `ContextBuilder` now renders a single `## Identity (Code-Owned)` section and removes the trailing identity-guard section so prompt composition has one authoritative identity anchor.
2026-03-14: Bootstrap composition now includes per-layer interpretation text for `AGENTS.md`, `SOUL.md`, `USER.md`, and `TOOLS.md` while preserving user-authored markdown and continuing to emit `ContextDiagnostics` warnings for conflicts.
2026-03-14: Added prompt regression asserting valid SOUL persona/style guidance remains in the rendered prompt and does not trigger the `identity_declaration_in_soul` warning category.
2026-03-14: Added regressions that make defaults explicit: missing bootstrap files still produce a valid composed prompt with empty diagnostics, two session keys (`telegram:1`, `discord:2`) intentionally share workspace-global `USER.md` and `memory/MEMORY.md`, and session histories remain key-scoped.
2026-03-14: Added onboarding-plus-composition regression proving legacy conflicting USER/MEMORY content is tolerated and diagnosed (not mutated), preserving file bytes while prompt assembly continues to include content under boundary interpretation.
15: 2026-03-14: Updated onboarding templates so new workspaces receive ownership-aligned content. AGENTS.md no longer includes an Identity section (now code-owned in ContextBuilder). SOUL.md no longer contains "I am Nex Agent" declarations and focuses solely on persona/values/style. USER.md emphasizes profile and collaboration preferences.
16: 2026-03-14: Extended onboarding migration tests to verify: new templates do not encode identity replacement; new templates align with runtime contract; existing customized user files (SOUL.md, USER.md) are preserved during initialization; managed templates (AGENTS.md, TOOLS.md) merge without overwriting user content outside managed blocks.
17: 2026-03-14: All onboarding tests pass and compile succeeds, confirming template changes are backward compatible and preservation guarantees remain intact.
