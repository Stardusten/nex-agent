# 2026-04-24

- review 并重写 `phase10d-self-update-deploy-control-plane.md`，把原文里未完成的 stage placeholder 补成完整 task-plan。
- 新增 `phase10e-code-editing-toolchain-reset.md`，明确下一步不在旧 `read/edit/write` 上补兼容，而是直接重置为 `find -> read -> apply_patch -> self_update` 的新工具主链。
- 新增 `phase10f-self-iteration-ux-and-release-visibility.md`，承接 10e 后剩余的 agent 卡点：统一 inspect 心智、补强 `self_update status/history` 的 release 可见性、对齐 quick-check vs strict-check、并明确 owner/subagent handoff。
- 收紧了 phase10d 冻结 contract：
  - 明确 `self_update` 是唯一 CODE deploy 入口，但 `HotReload` 只提供逐文件 compile/load，返回语义只能是 `runtime_restored: :none | :best_effort`，不再假装支持事务式 runtime rollback。
  - `protected_module?` 扩展到 `SelfUpdate.Planner`、`SelfUpdate.Deployer`、`SelfUpdate.ReleaseStore`，避免新控制链暴露给自修改。
  - `CODE` 层范围明确限定为 repo `lib/nex/agent/**/*.ex`，不把 workspace custom tool 混进 phase10d deploy 面。
  - `Admin` / `console` / `reflect` 的迁移改成明确二选一收口，不再用“迁移或删除”这种模糊说法。
- 补齐 Stage 1-4 的前置检查、改动文件、实施内容、验收和验证，并新增 `Review Fail 条件`，使文档满足仓库 `docs/dev/task-plan` 格式规范。
- 开始实现 phase10e：
  - 删除旧 `edit` / `write` / `list_dir` tool，不保留兼容入口。
  - 新增 `find` / `apply_patch`，并把 `read` 升级为结构化返回，带分页与 continuation 元信息。
  - 更新 `Registry` / `FollowUp` / contract tests，使 `apply_patch` 不再泄漏到 `:follow_up`，并让 `find` 成为默认代码发现路径。
  - 把 onboarding、runtime prompt、console 文案从“写文件就热更新”切到 `find -> read/reflect -> apply_patch -> self_update deploy`。
- 新增 Phase 11 自愈驱动总计划：
  - 明确 Phase 11 不是一次性重构，而是探索式自迭代驱动系统。
  - 把目标收敛为“建立低成本、预算驱动、可逐步扩展的自愈最小闭环”。
  - 冻结三条 lane：hot-path deterministic recovery、near-path bounded reflective recovery、long-path evolution。
  - 引入类似人体代谢的 energy ledger，让不同能量储备控制自迭代频率、强度和范围。
  - 明确 token 控制原则：失败先结构化记录和 cheap aggregation，不允许每次失败都调用 LLM。
- 新增 Phase 11A 最小自愈闭环计划：
  - 第一版只接 `tool.call.failed`、`llm.call.failed`、`self_update.deploy.failed` 三类事件。
  - 新增 workspace 内 `self_healing/runtime_events.jsonl` 和 `self_healing/energy.json` 作为最小 durable state。
  - 11a 只做 event store、energy ledger、aggregator、router，不做 LLM reflection，不写 memory/skill/code，不自动 deploy。
- 开始并落地 Phase 11A 实现：
  - 新增 `Nex.Agent.SelfHealing.EventStore`、`EnergyLedger`、`Aggregator`、`Router`。
  - Runner 在最终 LLM 失败和 tool result 为 `Error:` 时记录结构化事件。
  - `SelfUpdate.Deployer` 在 deploy 返回 failed result 时记录 `self_update.deploy.failed`，不改变主返回语义。
  - Runtime prompt / onboarding 只说明 11a 的真实能力：记录失败信号、给出 hint/reflection candidate，不自动写 memory/skill/code，不绕过 owner deploy。
  - 补充 self-healing event store、energy ledger、aggregator、router 单元测试，以及 Runner / self_update / ContextBuilder 覆盖。
- 落地 Phase 12 LLM provider adapter 架构：
  - 新增 `ProviderAdapter` behavior、`ProviderRegistry` 和 `Nex.Agent.LLM.Providers.*` 独立 adapter。
  - `ProviderProfile` 收口为 profile struct + facade，不再承载 openai-codex/openrouter/ollama 等 provider policy。
  - `ProviderAdapter` callbacks 允许走 default adapter fallback，薄 adapter 不需要保留无意义转发函数。
  - `ReqLLM` 主链只通过 `ProviderProfile` facade 获取 message/options、auth、provider_options、model_spec 和 stream fun。
  - provider 默认模型选择也进入 adapter contract，`ReqLLM` 不再维护 provider-specific default model 分支。
  - 将 OpenAI Codex stream/responses policy 迁到 `providers/openai_codex/` namespace，删除 root-level 内部模块。
  - 补齐 provider registry/profile 和 OpenAI Codex OAuth/API-key 分叉 focused tests。
- 规划 Phase 13 Control Plane Observability：
  - 推翻 logs/events 两套主链的设计，冻结 observation 作为机器真相源，human log 只是 projection。
  - 收紧必要版本：`context` 只放 workspace/run/session/channel/tool_call 等关联身份，`attrs` 只放业务参数。
  - 冻结最小 API：`ControlPlane.Log`、`Metric`、`Gauge`、`Budget`、`Store`、`Query` 和唯一 agent-facing `observe` tool。
  - 明确 13A 先替换 Phase 11A self-healing event/energy 主链，不保留双写兼容。
- 落地 Phase 13A 最小 ControlPlane observability cutover：
  - 新增 `Nex.Agent.ControlPlane.{Store,Redactor,Query,Log,Metric,Gauge,Budget}`，JSONL observation 落在 workspace `control_plane/observations/`，gauge/budget state 落在 `control_plane/state/`。
  - 新增唯一 agent-facing `observe` tool，接入 `:all`、`:base`、`:follow_up`，支持 summary/query/tail/metrics/incident，且不接受任意 path。
  - 删除 `SelfHealing.EventStore` / `SelfHealing.EnergyLedger` 旧主链和测试；Runner / SelfUpdate / Router 的 11A 三类失败改写 ControlPlane observation。
  - Runtime prompt / onboarding 改为引导使用 `observe` 查询控制平面；budget 只控制复盘/候选信号，不允许自动 deploy 或写 memory/skill/code。
  - 修复 13A review 发现的 redaction/context 边界：Log projection 使用 Store 已脱敏 observation，Gauge state 持久化前脱敏，Store normalization 从 `attrs` 删除 workspace/run/session/channel/tool_call 等 context identity key。
  - 补充 Log/Gauge/Store 回归测试，覆盖日志投影脱敏、gauge state/summary 脱敏、attrs 不复制 context identity。
  - 验证通过：ControlPlane/observe/tool surface/router/runner/context focused 组合 + `self_modify_pipeline_test.exs:182`；`self_modify_pipeline_test.exs` 全文件仍会互相改 tracked fixture，需单测目标用例或先隔离 fixture。
- 新增 Phase 13B runtime lifecycle observability 计划：
  - 明确 13B 开工前必须先确认 13A redaction/context findings 已修复，不能把生命周期观测建在会泄密的底座上。
  - 冻结 Runner run/LLM/tool batch/tool call、Tool.Registry execute、HTTP request、SelfUpdate deploy 的点分 tag contract。
  - 把 `Nex.Agent.HTTP` 的 internal opts 泄漏列为硬验收：`:cancel_ref` / observe opts 必须在调用 `Req` 前剥离，request task exception 必须转成结构化 error 并写 `http.request.failed`。
  - 将 13B 拆成可测试 stage：13A preflight、Runner LLM lifecycle、Runner tool lifecycle、Tool.Registry execution、HTTP lifecycle、SelfUpdate lifecycle、docs/prompt 收尾。
- 新增 Phase 13C Run Control / Follow-Up observability 计划：
  - 13C 开工前明确要求先修复 13B review findings：failed tool evidence args 脱敏限长、Registry timeout owned emit path、Gauge redaction 测试保持。
  - 冻结 `run.owner.current` 为 workspace-level gauge，不使用 per-session 动态 gauge name；value 包含当前 workspace 所有 active owners 的 phase/tool/elapsed/queued/tail。
  - 冻结 RunControl / InboundWorker 观测 tags，把 owner lifecycle、owner dispatch、follow-up dispatch、queue、interrupt、status request 纳入 ControlPlane。
  - 明确 `/status` 仍是 deterministic command，但必须使用 RunControl + ControlPlane recent evidence；follow-up 在回答报错/卡住/后台状态类问题前必须先用 `observe summary` 或 `observe incident`。
- 新增 Phase 13D Semantic Log / Admin Query cutover 计划：
  - 13D 接住“现有日志语句全部迁移”的工作：语义 `Logger.*` 必须迁到 ControlPlane，剩余 direct Logger 只能是 projection/fallback/boot/第三方边界并由 allowlist 测试保护。
  - 13D 同时收口 Audit / RequestTrace / Admin，停止把 `audit/events.jsonl` 和 `audit/request_traces/*.jsonl` 当机器真相源。
  - Admin recent events、request trace、incident/status view 必须读 ControlPlane Query，并和 `observe` 对齐到同一 observation ids。
  - 13D 完成后 13E 才能开始，避免 Evolution 消费半真相源。
- 新增 Phase 13E Evolution ControlPlane consumption 计划：
  - 13E 明确依赖 13D 完成，不在 Audit/RequestTrace/Admin 和 semantic Logger 仍未收口时让 Evolution 消费半真相源。
  - 冻结 Evolution evidence 只来自 ControlPlane Query/Gauge/Budget；`memory/patterns.jsonl`、旧 EventStore、旧 EnergyLedger、raw log file、Audit 私有文件都不能再作为机器真相源。
  - 定义 evidence pack、candidate action、`evolution.*` tags 和 budget mode 行为；sleep/low 不调用 LLM，normal/deep 才允许 bounded reflection。
  - 明确 `run_evolution_cycle/1` 只返回带 evidence ids 的候选动作，不自动写 SOUL/MEMORY/skills/code，也不调用 deploy。
  - 补入 13E Stage 0 pause：进入 evolution evidence/budget/candidate 主线前，先做一次 13D residual semantic Logger closeout，复核 logger allowlist，清掉 remaining machine-truth direct Logger。
- 开始并落地 Phase 13B runtime lifecycle observability：
  - Runner 写入 `runner.run.*`、`runner.llm.call.*`、`runner.tool.batch.*`、`runner.tool.call.*` 与 task exit/timeout/cancel lifecycle observation，失败仍复用 13A 的 `runner.llm.call.failed` / `runner.tool.call.failed` tag。
  - `Tool.Registry.execute/3` 写入 deterministic execution started/finished/failed/cancelled observation，并在 `cancel_run/1` 时回复原 execute caller，避免只留下 GenServer call timeout。
  - `Nex.Agent.HTTP` 在调用 Req 前剥离 `:cancel_ref` / `:observe_context` / `:observe_attrs`，写入 request started/finished/failed/timeout/cancelled observation，并把 request task exception 转成 `{:error, {:exception, class, message}}`。
  - `web_fetch` / `web_search` 把 tool ctx 里的 workspace/run/session/channel/chat/tool_call 传入 HTTP observe context。
  - `SelfUpdate.Deployer.deploy/3` 补齐 started/finished/failed lifecycle observation，失败 attrs 同时保留 router 消费所需结构。
  - Runtime prompt / onboarding 更新为说明 `observe` 可查 run、LLM、tool、HTTP、self_update lifecycle。
- 落地 Phase 13C RunControl / FollowUp / InboundWorker observability：
  - `RunControl` 写入 `run.owner.started/updated/finished/failed/cancelled/stale_result`，并在每次 owner state 变化后刷新 workspace-level `run.owner.current` gauge。
  - `run.owner.current` gauge 保持 `owners` 列表 shape，覆盖多 session active owner、phase/tool/elapsed/queued、assistant/tool tail，tail 经过 ControlPlane redaction 且 bounded。
  - `InboundWorker` 写入 inbound received、owner dispatch started/finished/failed/timeout、follow-up started/finished/failed、queue changed、interrupt requested、status requested observations。
  - `/status` 仍不触发 LLM，输出 RunControl deterministic snapshot，并追加当前 run/session 的 recent warning/error evidence line。
  - Follow-up prompt 明确要求错误、卡住、后台状态、日志、status、incident 类问题先用 `observe summary` / `observe incident` / `observe query`，并保持 follow-up tool surface 只读 + interrupt。
  - 覆盖测试：`run_control_test.exs`、`inbound_worker_test.exs`、新增 `follow_up_test.exs`，包含 success/failure/cancel/stale、timeout/crash、/status evidence、follow-up observe guidance、tool surface 冻结。
- 落地 Phase 13D Semantic Log / Admin Query cutover：
  - `ControlPlane.Query` 增加 observation summary、recent event summary、run trace derived view，并补充 tag prefix / kind / channel / chat / trace filters。
  - `Audit.append/3` 不再写 `audit/events.jsonl`，只作为 `ControlPlane.Log.info/3` 兼容 wrapper；生产调用点改为直接写 `ControlPlane.Log`。
  - `RequestTrace.append_event/2` / `list_paths/1` / `read_trace/2` 不再写读 `audit/request_traces/*.jsonl`，改为 ControlPlane-derived compatibility view；Runner trace attrs 改为 bounded summaries。
  - Admin recent events、evolution/memory/code recent events、runtime request trace summary/detail 改读 `ControlPlane.Query`，不再依赖 Audit/RequestTrace 私有文件。
  - ContextBuilder / onboarding 删除 workspace `audit/events.jsonl` 作为 agent guidance，改指向 `observe` 与 workspace `control_plane/`。
  - 新增 `control_plane_logger_cutover_test.exs` 锁定 direct Logger allowlist；剩余 direct Logger 是 projection/fallback/boot/channel/third-party boundary，不作为 Admin/Evolution 机器证据。
  - 覆盖测试：`admin_test.exs`、`request_trace_test.exs`、`evolution_test.exs`、`runner_evolution_test.exs`、`runtime_test.exs`、`observe_tool_test.exs`、`control_plane_store_test.exs`、`tool_alignment_test.exs`、`context_builder_test.exs`、`control_plane_logger_cutover_test.exs`。
- 开始落地 Phase 13E Evolution ControlPlane consumption：
  - `Nex.Agent.Evolution` 不再读写 `memory/patterns.jsonl`，`record_signal/2` 改为只写 `evolution.signal.recorded` observation，`run_evolution_cycle/1` 改为返回 candidate-oriented result，不再自动写 SOUL/MEMORY/skills/code。
  - 新增 `Nex.Agent.Evolution.Evidence`，只用 `ControlPlane.Query` / `Gauge` / `Budget` 构建 evidence pack，包含 bounded observations、repeated patterns、current runs、budget 和 candidate history。
  - `SelfHealing.Aggregator` 改为聚合 observation summary envelope，`Router` 也改读 ControlPlane summary，不再依赖旧 self-healing event shape。
  - `reflect`、`admin`、`mix nex.agent evolve` 改成展示 recent signal observations / candidate history，而不是 pending `patterns.jsonl` 状态。
  - `Admin.Event` 的 evolution 文案改到新点分 tag：`evolution.cycle.*`、`evolution.pattern.detected`、`evolution.candidate.proposed`。
  - focused 验证通过：`mix test test/nex/agent/evolution_test.exs test/nex/agent/evolution_integration_test.exs test/nex/agent/self_healing_aggregator_test.exs test/nex/agent/self_healing_router_test.exs test/nex/agent/admin_test.exs test/nex/agent/context_builder_test.exs test/nex/agent/control_plane_logger_cutover_test.exs`
- 开始落地 Phase 14 Owner-Approved Evolution Execution：
  - 新增 `Nex.Agent.Evolution.Candidates`，把 `evolution.candidate.*` observations reduce 成单一 derived candidate view，不新增 state file、审批队列表或长期缓存。
  - `Nex.Agent.Evolution` 新增 `recent_candidates/1` 和 `candidate/2` 只读入口，继续以 ControlPlane 作为 candidate lifecycle 的机器真相源。
  - 新增 owner-only deterministic tool `evolution_candidate`，支持 `list/show/approve/reject`；`approve/reject` 先只写 lifecycle observations，不在 tool 内复制 memory/soul/skill/code apply 逻辑。
  - `evolution_candidate approve` 对 `code_hint` 默认 `mode=plan`，非 code candidate 默认 `mode=apply`，但当前还未接 realization/apply lane。
  - `Registry` / `ToolList` 已接入 `evolution_candidate`，并保持 follow-up / subagent surface 不暴露该 tool。
  - focused 验证通过：`mix test test/nex/agent/evolution_test.exs test/nex/agent/tool_alignment_test.exs`
- 继续推进 Phase 14 realization/apply 与展示层：
  - 新增 `Nex.Agent.Evolution.Executor`，把 approved candidate 映射到既有 deterministic 主链：`memory_candidate -> memory_write`、`soul_candidate -> soul_update`、`skill_candidate -> skill_create`；`code_hint` 先稳定 `mode=plan` realization。
  - `evolution_candidate approve` 现在会补齐 `evolution.candidate.realization.generated`、`apply.started`、`apply.completed` / `apply.failed` observations；非 code candidate 在 owner approval 后可直接沿既有 tool lane 落地。
  - `SoulUpdate.execute/2` 修正为尊重 `ctx.workspace`，避免 candidate executor 走 deterministic lane 时退回全局 workspace。
  - `Admin.evolution_state/1`、`overview_state/1` 与 `Reflect` 的 evolution status/history 现在展示 derived candidate lifecycle，而不再只看到 proposed events。
  - focused 验证通过：`mix test test/nex/agent/evolution_test.exs test/nex/agent/evolution_integration_test.exs test/nex/agent/admin_test.exs test/nex/agent/tool_alignment_test.exs test/nex/agent/memory_write_test.exs test/nex/agent/soul_update_validation_test.exs`
- 完成 Phase 14 Owner-Approved Evolution Execution：
  - `code_hint mode=apply` 现在也会走既有 CODE lane 形状：通过 bounded code realization 生成 patch proposal，复用 `apply_patch` 与 `self_update deploy`，不新增平行 deploy/patch 系统。
  - `Evolution.emit_candidate_observations/3` 会对同 kind/same summary 的旧 pending candidate 写 `evolution.candidate.superseded`，把 lifecycle tags 补齐到 task-plan 冻结的全集。
  - `ContextBuilder` / onboarding 文案已对齐：evolution 先提 candidate，owner 通过单一 `evolution_candidate` lane 审批执行，code candidate 仍受 `self_update` deploy authority 约束。
  - `Admin.Event` 新增 candidate approved/rejected/realization/apply 相关摘要，Admin / Reflect / Observe 对同一 candidate id 看到的是同一条 ControlPlane lifecycle。
  - focused 验证通过：`mix test test/nex/agent/evolution_test.exs test/nex/agent/evolution_integration_test.exs test/nex/agent/admin_test.exs test/nex/agent/tool_alignment_test.exs test/nex/agent/context_builder_test.exs test/nex/agent/memory_write_test.exs test/nex/agent/soul_update_validation_test.exs`
- 新增 Phase 15 Provider-Native Tool Capability Resolution 计划：
  - 确认 `openai-codex` 官方 OAuth backend 可以接受 built-in `web_search`，且真实请求的 usage 里出现 `tool_usage.web_search.count == 1`。
  - 明确 `web_search` 不应继续停留在“静态 local tool + provider payload ad-hoc rewrite”状态，而要提升为 capability-resolved tool。
  - 冻结 Phase 15 边界：先做统一 capability resolver，第一目标只覆盖 `web_search`，至少支持 `openai-codex` 官方 OAuth backend 的 native search；不在本 phase 顺手接 `image_generation` 或其他 provider-native tool。
  - 将 `ProviderProfile.default_api_key(:openai_codex)` facade 偏差列为 Phase 15 必修项，避免 provider profile 真相源和 `Auth.Codex.resolve_access_token/0` 分裂。
- 新增 Phase 14 Owner-Approved Evolution Execution 计划：
  - 在不新增 candidate 平行状态文件的前提下，把 13E 的 `evolution.candidate.proposed` 接到统一的 owner-only execution lane。
  - 冻结单一 owner-facing tool `evolution_candidate`，最小动作是 `list/show/approve/reject`，follow_up / subagent 不暴露 approve/reject/apply。
  - candidate lifecycle 继续只用 ControlPlane observations 表达，追加 `approved/rejected/realization.generated/realization.failed/apply.started/apply.completed/apply.failed/superseded`。
  - candidate execution 必须复用现有 deterministic 主链：memory/soul/skill 走对应 tool，code 走既有 `find/read/apply_patch/self_update` CODE lane，不重写一套 apply 逻辑。
  - `code_hint` 默认 `approve -> mode=plan`，避免在 Phase 14 一上来就绕过 owner 和 deploy authority。
- 开始实现 Phase 15 provider-native tool capability resolution：
  - 新增 `Nex.Agent.Tool.Capability` / `CapabilityResolver`，把 `web_search` 的 definition、execution strategy、provider-native emission 收到同一 resolver。
  - `Tool.Registry.definitions/2` 与 `Runtime` snapshot 现在按 provider/config/surface 解析最终 tool definition；官方 `openai-codex` OAuth backend 会暴露 native `web_search` built-in，其余 provider 继续走本地 `web_search`。
  - `ReqLLM` 现在可以直接透传 resolver 产出的 provider-native built-in tool shape，不再依赖 `ResponsesPolicy` 里按 tool name 做 ad-hoc rewrite。
  - `Config.web_search_capability/1` 新增统一 accessor，规范化 `strategy`、`mode`、`allowed_domains`、`user_location`；`Config` 的 codex api key/base_url fallback 也改为复用 `ProviderProfile` facade。
  - 修复 `ProviderProfile.call_adapter/3` 未 `Code.ensure_loaded/1` 导致 `default_api_key(:openai_codex_custom)` 误回退 default adapter 的问题。
  - focused 验证通过：`mix test test/nex/agent/tool/capability_resolver_test.exs test/nex/agent/llm/providers/openai_codex_test.exs test/nex/agent/tool_alignment_test.exs test/nex/agent/config_test.exs test/nex/agent/follow_up_test.exs test/nex/agent/llm/req_llm_test.exs`
