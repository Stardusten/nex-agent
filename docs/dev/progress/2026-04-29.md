# 2026-04-29

## Phase 18D Notes App CORS Follow-up

问题：

- Notes app iframe 使用 `sandbox="allow-scripts"`，因此 iframe document 是 opaque origin。
- Vite/ES module assets 从 `/app-assets/notes/dist/assets/*` 加载时会走 CORS。
- Workbench asset response 之前没有 CORS header，浏览器表现为静态资源 200 但 module/CSS 被 Same Origin Policy 拦截，页面空白。

实现：

- `Nex.Agent.Workbench.Server` 的 `{:asset, ...}` response 增加 app asset 专用 headers：
  - `Access-Control-Allow-Origin: *`
  - `Access-Control-Allow-Methods: GET, OPTIONS`
  - `Cross-Origin-Resource-Policy: cross-origin`
- 没有改 iframe sandbox 为 `allow-same-origin`，避免削弱 iframe 隔离。
- 没有把 Workbench JSON/API bridge CORS 放宽；修复只作用于静态 app assets。
- `server_test` 覆盖 `/app-assets/:id/*` headers。

验证：

```bash
/opt/homebrew/bin/mix test test/nex/agent/workbench/server_test.exs test/nex/agent/workbench/assets_test.exs
/opt/homebrew/bin/mix compile --warnings-as-errors
```

结果：

- Workbench server/assets tests: 18 tests, 0 failures
- `compile --warnings-as-errors`: passed

现场注意：

- 尝试重启真实 gateway 以应用新代码时，Workbench 一度监听 `127.0.0.1:50051`，但 Feishu WS endpoint 请求随后失败为 `%Req.TransportError{reason: :nxdomain}`，gateway task 退出。
- 这次退出不是 CORS 修复导致；需要在外部网络/DNS 可用时重新启动 gateway 再刷新 Workbench。

## Phase 18D Notes App Interaction Follow-up

实现：

- Notes app workspace artifact 增加 1 秒 debounce autosave；权限/冲突/保存错误时不会持续重试。
- 左侧 Markdown 列表改成按 vault-relative path 分组的可折叠文件树。
- Workbench Notes app 外层固定到 viewport，左侧文件树和右侧 ProseMark editor 独立滚动。
- 编辑器内 ProseMark image widget 限制为 `max-width: 100%`，避免大图撑出 viewport；scope 只作用于 `.cm-image img`，避免破坏 CodeMirror 内部 `img.cm-widgetBuffer`。

验证：

```bash
cd /Users/krisxin/.nex/agent/workspace/workbench/apps/notes
./reload.sh
./node_modules/.bin/tsc --noEmit
rm -rf node_modules
```

结果：

- Vite build passed，dist assets 已刷新。
- TypeScript check passed。

## Phase 18D Workbench App Chrome Follow-up

实现：

- Workbench app manifest 新增 `chrome.topbar` contract：
  - 默认 `%{"topbar" => "auto"}`。
  - 允许 `"auto"` 或 `"hidden"`。
- Workbench shell 不再按 app id 写死 Notes 行为，而是按当前 app manifest 的 `chrome.topbar` 切换顶栏显示。
- `Notes` app 的 `nex.app.json` 配置为 `"chrome": {"topbar": "hidden"}`，进入 Notes 时保留左右边缘抽屉，但完全不显示顶部 app chrome。
- shell view 切换逻辑整理为 `stageIds` / `inspectorIds` / `navIds` mapping，减少新增 view 或 chrome 行为时的重复 DOM 状态更新。

验证：

```bash
awk '/<script>/{flag=1; next} /<\/script>/{flag=0} flag {print}' priv/workbench/shell.html | node --check
/opt/homebrew/bin/mix test test/nex/agent/workbench
/opt/homebrew/bin/mix compile --warnings-as-errors
```

结果：

- Workbench tests: 39 tests, 0 failures
- `compile --warnings-as-errors`: passed
- live `GET /api/workbench/apps` 返回 Notes `chrome.topbar = hidden`。
