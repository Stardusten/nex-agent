# 2026-04-23

- 为 `Nex.Agent.Runner` 增加 stream 中断后的 partial content 保留与 continue prompt 续写重试；仅在已收到可见 partial 且错误满足 transient 条件时触发。
- stream drain 现在把中断错误包装成带 `partial_content` 的结构化错误；最终失败时 `%Nex.Agent.Stream.Result{}` 也保留 partial content，避免用户侧完全丢失已生成文本。
- 新增 `runner_stream_test` 覆盖：首轮 stream 在 `"hello "` 后因 transport error 中断，第二轮必须收到 injected continue prompt，用户侧事件只收到续写部分 `"world"`，最终 session 内容为完整 `"hello world"`。
- 验证：`/opt/homebrew/bin/mix test test/nex/agent/runner_stream_test.exs test/nex/agent/llm/req_llm_test.exs`
