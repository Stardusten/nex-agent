# 2026-04-17

## Cross-platform slash command foundation

- 新增统一 command 层，command 真相源不再散落在 `InboundWorker` 文本分支里
- runtime snapshot 增加 `commands` 字段，command definitions 跟随 runtime reload
- `InboundWorker` 先做 command resolve，再决定是否进入 busy queue / LLM
- 现有 `/new`、`/stop` 已收编到 command catalog
- 新增 `/commands`，用于文本 fallback 场景下向 IM 用户展示当前支持的 slash commands
- 明确保留未注册 slash 文本原样进入 LLM 的 contract，避免抢占普通 prompt

## 验证

```bash
mix test test/nex/agent/runtime_test.exs test/nex/agent/inbound_worker_test.exs test/nex/agent/tool_alignment_test.exs
mix compile
```

## 后续

- 把 approval slash commands 接到统一 command catalog
- 让 Discord 基于 runtime command catalog 做 native registration / autocomplete
